#!/usr/bin/env bash
set -euo pipefail

npm run lint
npm run fmt:check
npx tsc -b --pretty false

if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged
else
  echo "gitleaks not installed; skipping staged secret scan." >&2
fi
